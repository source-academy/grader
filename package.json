{
  "name": "grader",
  "version": "0.1.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "./scripts/build.sh",
    "typecheck": "tsc --noEmit",
    "test": "jest --env=node",
    "test-coveralls": "jest --env=node --coverage --collectCoverageFrom=src/index.ts --coverageReporters=text-lcov --forceExit | coveralls"
  },
  "dependencies": {
    "@types/node": "^10.14.1",
    "js-slang": "0.1.14",
    "typescript": "^3.3.3333"
  },
  "devDependencies": {
    "@types/jest": "^24.0.9",
    "coveralls": "^3.0.2",
    "jest": "^23.4.0",
    "ts-jest": "^23.0.0"
  },
  "jest": {
    "moduleFileExtensions": [
      "ts",
      "js"
    ],
    "transform": {
      "\\.ts$": "ts-jest"
    },
    "testRegex": "/__tests__/test.*\\.ts$"
  }
}
